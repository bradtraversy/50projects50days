language: node_js
node_js:
  - 7

script:
  - ./configure && make

notifications:
email:
  recipients:
    - kahmeng456@gmail.com
  on_success: always # default: change
  on_failure: never # default: always